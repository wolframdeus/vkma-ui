import {PanelTransitionTheme} from './types';

export const panelTransitionBrightLightTheme: PanelTransitionTheme = {
  colors: {suspender: 'rgba(0,0,0,.5)',},
};

export const panelTransitionSpaceGrayTheme: PanelTransitionTheme = {
  colors: {suspender: 'rgba(0,0,0,.5)',},
};
