/**
 * Global styles
 */
export interface Global {
  backgroundColor: string;
  text: {
    fontFamily: string;
    colors: {
      primary: string;
      secondary: string;
    };
  };
}
